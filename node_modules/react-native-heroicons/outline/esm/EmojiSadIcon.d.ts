import { SvgProps } from "react-native-svg";
interface Props extends SvgProps {
    size?: number;
}
declare const EmojiSadIcon: ({ size, ...props }: Props) => JSX.Element;
export default EmojiSadIcon;
