import { SvgProps } from "react-native-svg";
interface Props extends SvgProps {
    size?: number;
}
declare const ArchiveIcon: ({ size, ...props }: Props) => JSX.Element;
export default ArchiveIcon;
