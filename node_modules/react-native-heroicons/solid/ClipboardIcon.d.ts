import { SvgProps } from "react-native-svg";
interface Props extends SvgProps {
    size?: number;
}
declare const ClipboardIcon: ({ size, ...props }: Props) => JSX.Element;
export default ClipboardIcon;
