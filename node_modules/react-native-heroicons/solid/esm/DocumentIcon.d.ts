import { SvgProps } from "react-native-svg";
interface Props extends SvgProps {
    size?: number;
}
declare const DocumentIcon: ({ size, ...props }: Props) => JSX.Element;
export default DocumentIcon;
